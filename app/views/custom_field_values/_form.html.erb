<%= form_for(@custom_field_value) do |f| %>
  <% if @custom_field_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custom_field_value.errors.count, "error") %> prohibited this custom_field_value from being saved:</h2>

      <ul>
      <% @custom_field_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_id %><br>
    <%= f.text_field :contact_id %>
  </div>
  <div class="field">
    <%= f.label :custom_field_id %><br>
    <%= f.text_field :custom_field_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
