<p id="notice"><%= notice %></p>

<h1>Listing Custom Fields</h1>

<table>
  <thead>
    <tr>
      <th>Custom field type</th>
      <th>Name</th>
      <th>Description</th>
      <th>Default</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @custom_fields.each do |custom_field| %>
      <tr>
        <td><%= custom_field.custom_field_type %></td>
        <td><%= custom_field.name %></td>
        <td><%= custom_field.description %></td>
        <td><%= custom_field.default %></td>
        <td><%= link_to 'Show', custom_field %></td>
        <td><%= link_to 'Edit', edit_custom_field_path(custom_field) %></td>
        <td><%= link_to 'Destroy', custom_field, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Custom field', new_custom_field_path %>
